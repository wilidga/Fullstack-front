const types = {
  refreshData: "[Posts] - refresh-data",
  deleteData: "[Posts] - delete-data",

  auth: "[User] - authentication",
  login: "[User] - login",
  logout: "[User] - logout",
};

export default types;
